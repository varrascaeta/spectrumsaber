# Overrides for testing (loaded after common.env)

# App
ENV=testing

# Database (test DB — overrides common.env)
POSTGRES_DB=postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=9432

# FTP (mock path — overrides common.env)
BASE_FTP_PATH=/test/ftp

# Python
PYTHONPATH=$PWD/src
